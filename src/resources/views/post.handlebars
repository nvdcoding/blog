<article>
    <div class="container">
      <div class="row">
        <div class="col-lg-10 col-md-12 mx-auto">
          <h2>{{post.title}}</h2>
          <p class="text-xs-left"><i>{{post.description}}</i></p>
          {{{post.content}}}
        </div>
      </div>
    </div>
  </article>